# vault/ — Obsidian 원본

이 폴더가 Obsidian vault 루트입니다. 여기서 글을 씁니다.

**지금 들어있는 파일은 파이프라인 검증용 미끼(decoy)입니다.**
저장소가 아직 public이라 실제 vault를 넣지 않았습니다.
GitHub Pro 구독 후 저장소를 private으로 전환하는 시점에 실제 vault로 교체합니다.

frontmatter에 `publish: true`(boolean)를 넣은 `.md`만 사이트에 올라갑니다.
